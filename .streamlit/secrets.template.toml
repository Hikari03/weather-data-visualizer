# Replace it with your gmail email address, under which you will be able to administer the app.
admin_email = "xxxx@yyy.zz"

[connections.db]
# When not using docker:
# Replace <database_engine> with the name of your database, for example, postgres.
# url = "<database_engine>://<username>:<password>@<adress>:<port>/<database_name>"
url = "postgresql://postgres:varilamysickakasicku@db:5432/weather_data" # if you are using docker, do not edit


# See https://docs.streamlit.io/develop/concepts/connections/authentication and https://developers.google.com/identity/openid-connect/openid-connect
# for setup instructions.
# You don't have to use Google.
[auth]
redirect_uri = "http://<adress from user point of view>:<port>/oauth2callback"
client_id = ""
client_secret = ""
cookie_secret = ""
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"

